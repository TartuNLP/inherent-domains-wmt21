Set variables and run `get_datasets.sh` to download corpora from OPUS and ParaCrawl, separate them by document ID, and make train, dev and test sets

Required Python packages: `opustools`, `numpy`
