Example files for preprocessing, training and fine-tuning. When running, be careful to set all necessary variables beforehand

Required Python packages: `fairseq`, `sacrebleu`, `sentencepiece`, `tensorboard`
